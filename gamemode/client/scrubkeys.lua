// 'softbinds' keys 1-4 for new players

local onedown 	= false
local twodown 	= false
local threedown = false
local fourdown 	= false

/*hook.Add("Think", "pk_scrub_keys", function()
	if input.IsKeyDown(KEY_1) && !onedown then
		onedown = true
		RunConsoleCommand("gm_spawn", "models/props/de_tides/gate_large.mdl")
	elseif !input.IsKeyDown(KEY_1) then
		onedown = false
	end
	if input.IsKeyDown(KEY_2) && !twodown then
		twodown = true
		RunConsoleCommand("gm_spawn", "models/props_canal/canal_bars004.mdl")
	elseif !input.IsKeyDown(KEY_2) then
		twodown = false
	end
	if input.IsKeyDown(KEY_3) && !threedown then
		threedown = true
		RunConsoleCommand("gm_spawn", "models/props_phx/wheels/moped_tire.mdl")
	elseif !input.IsKeyDown(KEY_3) then
		threedown = false
	end
	if input.IsKeyDown(KEY_4) && !fourdown then
		fourdown = true
		RunConsoleCommand("gm_spawn", "models/XQM/CoasterTrack/slope_225_3.mdl")
	elseif !input.IsKeyDown(KEY_4) then
		fourdown = false
	end
end)*/
